# Kepler v0.4 — state-privileged encoder + fast-iteration program

**Status: FREEZING TODAY (2026-07-30 ~07:15). LIVE doc.** Research fan-out 01-05 ALL LANDED
(~06:00-06:25, `RESEARCH.md`). **Fast-pivot directive (Ishneet ~06:50): freeze on the research
rankings TODAY; the remaining v0.3 reads (un-pool ep50 ~10:30 UTC = 18:30 local, tool_hang ~16:30 UTC =
00:30 local) are running checks that can AMEND the freeze, not serialization points. E2 killed at ep50-spread verdict —
the screening bar now comes from a dedicated calibration run, not E2 ep100.** Plain-language
live tracker: `../PROGRESS.md`.

## 0. The two decisions this doc exists to make

1. **Architecture + losses** for the v0.4 encoder (JQ's direction: robot state is the
   privileged citizen — state conditions the cross-attention queries that extract from vision),
   plus the downstream insertion shape.
2. **A screening loop fast enough to test everything in parallel** (~3-5h/verdict, many arms at
   once), so the one expensive confirmatory run happens exactly once, at the end, on the winner.

## 0.5 Claims ledger — which claim each arm serves

"Encoder beats downstream" is not one claim. Every arm is tagged with the claim it tests, and
every claim keeps its STRONG baseline (never a strawman). Pre-registered program-level kill:
if the best v0.4 config wins none of A/B/F, the reactive-control use is dead → C + the
hardware-efficiency story are what remain.

| Claim | Statement | Must beat | v0.4 arm(s) |
|---|---|---|---|
| **A** | adding z makes the policy better | same policy w/o z, matched budget | StateAdd lead + controls (the main screen matrix) |
| **B** | z alone ≈ baseline at ~70× fewer tokens | dense-patch policy, parity | ONE z-only arm (old replace recipe, new encoder); v0.2 failed it 14-vs-32 |
| **F** | fused force > raw force where force matters | vision + raw-F/T concat | LADDER: rung 0 = tool_hang ft-vs-noft (tonight, NO encoder involved); rung 1 (the claim) built ONLY on a rung-0 pass: force-token encoder + future-force aux vs raw concat; rung 2 = mechanism (contact gating) |
| **C** | z better for prediction/planning | patch-token dynamics (DINO-WM) | PARKED — exit path, no current harness |

Bookkeeping rule: every screen logs token-count-matched numbers (8×256 vs ~600 patches/cam) —
claim B / ARM-story evidence for free, even where claim A fails.

## 1. Why state-as-query (evidence already verified, RESEARCH.md (redesign section))

- AdapTac (strongest force-fusion result in corpus): privileged modality forms the cross-attn
  **queries** over other sensors' K/V — 93% vs 73% vision-only. BUT its own ablation shows the
  wiring alone loses to vision-only; the **future-prediction aux is the driver**. Query design
  and objective are ONE package.
- MCR (only frozen-encoder pretrain with real policy wins) is built on state-action grounding —
  its dynamics-alignment term is the largest ablation factor.
- Manipulation-centricity (attention on EE + task objects) predicts policy success (R≈0.93);
  state-conditioned queries are a mechanism that aims attention exactly there.
- Known failure mode: **proprio shortcut** (DiT-Block Policy needed per-dim state dropout) — on
  the encoder side this means queries extracting only state-predictable content. Mitigation +
  detection is a first-class design axis, not an afterthought.

## 2. Design axes (the matrix)

| Axis | Options (to be pruned by RESEARCH.md · ) |
|---|---|
| A. State entry | **RANKED by 01_state_as_query.md:** ① "StateAdd" — state→MLP(d_s→256→8·256), reshaped 8×256, added to the 8 learned queries through per-query tanh(α) gates, α zero-init (at α=0 ≡ v0.3 unpooled encoder exactly → finetunable from existing ckpt, no regression at init) · ② "StateQTok" — Q-Former wiring: 1-2 state tokens join the query stream via self-attn, masked OUT of vision cross-attn, dropped from output (InstructBLIP +4.3/+7.6 = best large-scale ablation) · ③ "StateFiLM" — adaLN-Zero on the query stream (DiT-winning mechanism, weakest state-specific evidence) · CONTROL arm: state as 1 extra K/V token (the equal-citizen scheme; no positive precedent, RDT token-drowning + GAP −15.8% argue against) · attention bias (EE-projection foveation) = later add-on only |
| B. Paired objective | **RANKED by 03_objectives.md:** ① action-conditioned FUTURE-VISION-LATENT prediction — target = frozen ViT-B/16 features of frame t+h (own backbone = free frozen target, DINO-WM/V-JEPA-2-AC pattern), cosine or L1, h screened {~0.3s, ~0.8s}, λ screened {0.05, 0.2}, predictor sees state+action chunk so the residual the loss chases is scene content ② same objective with DISCRETE targets (FSQ {5,3} or VQ K=256/d=128, cross-entropy — CE is part of the win; discrete beat continuous in EVERY ablated representation-target comparison; our failed E4 aux was continuous cosine) ③ state-BLIND inverse dynamics k=1 as additive term (head must NOT see state — else a_t ≈ f(s_t,s_{t+1}) in OSC space); swaps to AdapTac future-force (ℒ_π + α·ℒ_ffp, future-vs-observed = +20pp) if E6 tool_hang greenlights force. **PRE-REGISTERED KILLS:** future-STATE prediction (satisfiable from conditioning input; zero published support; Octo/GAP/copycat against) · temporal contrastive as primary · robot-keypoint tracks under state-conditioning (FK-derivable) |
| C. Loss attachment | **SETTLED by 03:** P≈8-32 DEDICATED predictor queries attending the same frozen K/V (+state), read-only isolation (not attended back by the 8 output queries), loss on predictor outputs only, final block — dedicated tokens win in every published instance (FRAPPE layer 21/28, FLARE layer 6/8, Octo readouts, 2605.04678 placeholders); on-output attachment has ZERO published positives + our E4 monotone-harm curve. Targets: frozen+stop-grad (EMA only needed if target is co-trained — FLARE ρ=0.995 slightly beats frozen but frozen still > baseline). ONE target family only (FRAPPE saw mode collapse with multiple streams) |
| D. Anti-shortcut | **RECIPE from 02_shortcut_mitigation.md (composable stack, front-loaded):** ① per-dim state dropout p=0.2 (DiT-Block, from code) ② NADA Gaussian state noise (σ* by Wasserstein train↔rollout matching; bootstrap RDT SNR ~20dB) ③ CFG-style condition dropout, p annealed 0.5→0.1 cosine over FIRST 50% of training then constant 0.1 (CFG: 0.1-0.2 optimal, 0.5 worse; GAP: suppression past 50% hurts; null = learned token or dataset-mean state per RDT) ④ zero-init gate on state injection ⑤ **4/4 query split — half state-conditioned, half never see state** (guaranteed state-independent channel + built-in diagnostic contrast). Escalation if collapse persists: GAP grad-attenuation λ=0.3 fixed ρ=0.5 first half → whole-state dropout p=0.8 (NADA baseline 54.6→74.1%). **From 01 (mandatory):** state stays OUT of the value pathway (GAP: state-in-content-path = −15.8% vs vision-only). DON'T: remove state entirely (needs wrist cams + relative-EE we lack), rely on vision aux for balance (GAP Aux loses), judge by val loss (NADA inversion: shortcut arm gets LOWER val loss, 2.94e-2 vs 4.13e-2, while success is 3% vs 53%) |
| E. Output shape | 8 tokens un-pooled (v0.3 un-pool arm arbitrates) · pooled (only if un-pool fails identically) |
| F. Downstream insertion | zero-init gated tokens (running now; gates confirmed ALIVE ep10→ep20) · SpawnNet-style adapters (fallback; module = `simple_bc/encoder/spawnnet/` MIT, portable per 05) |

**Key reframe from 01 (changes how we pitch this to JQ):** AdapTac's "force as query" is a
modality GATE (1 query vector, 2 K/V tokens, softmax = 2 mixing scalars), not state-driven
spatial attention over patches — our design extrapolates 2 K/V → ~600 patches/cam. No paper
runs the query-vs-K/V-vs-FiLM three-way; our matrix would be first. Triangulated evidence
still favors query-side entry (InstructBLIP, NoContactNoWorries F1 0.93-vs-0.74 at exactly
our dims d=256/frozen vision, DAB-DETR, pi0/GR00T/RDT all state-on-query-side at scale).
Zero-init gate on every new state path is non-negotiable (Flamingo tanh-gate +4.2, RT-1
identity-FiLM, DiT adaLN-Zero — and our own E5 history).

**Build-vs-copy (05):** copy clean — DiT-Block adaLN-zero mods + p=0.2 obs dropout
(`sudeepdasari/dit-policy` MIT), FACTR curriculum ~50 lines (`RaindragonD/factr` Apache),
SpawnNet adapters (MIT), MCR losses (MIT), AdapTac aux-head+force-query pattern (MIT),
Burns eval script. REIMPLEMENT (no license): FRAPPE prefix-token mechanism (4 small pieces:
token param, concat, depth tap, projector+cosine — `flappe_model.py` as reference),
2605.04678 discrete-target recipe. BUILD (no code): FLARE (use FRAPPE as code proxy),
Patch Policy (placeholder repo, 9 days old — re-check at freeze), MCR centricity metric
(Grad-CAM∩SAM-2, metric NOT in their repo).

Not a full cross-product: research (01-03) ranks per axis; we screen **top-1 per axis first**
(one lead config + one ablation per axis), expand only where a screen contradicts the ranking.

## 3. Fast-iteration protocol (screen cheap, confirm once)

Funnel — each stage kills before the next spends:

1. **Free offline pre-filter (~minutes, no policy run):** action-readout ladder (clean episode
   split), state R², RankMe, SeeSE3 dSE3@0.25s, + (pending agent-04 recipes) Burns CLS-attention
   Jaccard and MCR manipulation-centricity on the encoder ckpt. Probes are NECESSARY-not-
   sufficient here (v0.3 lesson: they invert as sufficiency signals) — used only as sanity
   floors + shortcut detectors (state-zeroing sensitivity).
2. **Encoder pretrain:** ~2-4h on the robocasa cache (E3 harness), co-residable 2-3/GPU.
3. **Policy screen (1 seed):** standard recipe, `checkpoint_every=10`; **gate at ep10-20** once
   the calibration data (ep10/20/30/40/50 curves now accumulating from un-pool + insertion arms)
   shows early rank predicts ep50 rank. Kill discipline per [[early-kill-discipline]]: gates
   pre-registered BEFORE numbers are seen.
4. **Fast eval for screens:** reduced-step diffusion inference (DDPM-16 / DDIM-16, validation
   run vs the known 66% square ckpt IN FLIGHT) + 50 rollouts. 25-rollout screens only for
   ≪-band kills (binomial noise at p≈0.5: ±2σ ≈ ±14pp @25, ±10pp @50 — 25 can kill, not rank).
5. **Confirm (top-2 only):** ep50+ep100, DDPM-100, 50 rollouts, 1 seed.
6. **THE BIG RUN (winner only):** 2-3 seeds, ep100+, standard eval, + transfer tasks
   (tool_hang/coffee if force is in the design). Nothing else ever gets multi-seed compute.

Throughput math: screen ≈ 2-4.5h train (ep10-20) + ~15-25min eval; 20GB arms co-reside 2/GPU on
the A6000s → ~10-14 concurrent screens box-wide when v0.3 runs clear ⇒ ~8-12 verdicts/day vs
current ~2-4. Encoder-side diagnostics (gate value off zero, state-zeroing delta) read straight
from ckpts at ep10 — a dead arm dies without a single rollout.

## 4. Sequencing / inputs pending

- **Un-pool vs insertion ep50 (today ~10:30):** decides E-axis, and whether v0.4 is an *upgrade*
  (un-pool recovers baseline → encoder content fine, redesign builds on it) or a *rescue*
  (still fails → state-conditioned extraction becomes the lead hypothesis). Both arms KILLED at
  ep50 regardless of outcome (fast-pivot); their every-10 curves double as the noise/early-rank
  calibration data.
- **E2 — KILLED 07-30 06:52 at ~ep80 (fast-pivot).** ep50 spread 8/12/18/32% = verdict on its
  own: DensePatch is an unreliable chassis on robocasa pnp (task/domain — same recipe = 66-72%
  Square). The screening bar is NOT E2 ep100; it comes from the calibration runs (baseline
  ckpt-every-10 + fast-eval trajectory + noise bands).
- **E6 tool_hang (tonight ~16:30-17:00):** claim-F rung 0 — whether force belongs in the v0.4
  objective set at all (square was NULL: 66/72 → 54/54). PRE-REGISTERED: kill both arms at the
  ep50 pair read unless ft−noft ≥ ~12pp; rung 1 (fused-vs-raw, the actual encoder claim) is
  built only on a pass.
- **RESEARCH.md design reports 01-05: ALL LANDED 07-30 ~06:00-06:25** (first launch was 429-killed; relaunch
  succeeded). Matrix in §2 updated from them; freeze now waits only on today's three v0.3 reads.

## 5. Pre-registered gates (to fill AT DESIGN FREEZE, before any v0.4 number is seen)

- Screen kill: ep10/ep20 fast-eval below <calibrated band vs dense curve> → kill. Gate math
  from 04: 25 rollouts kill at ≤7/25 vs p₁=0.5 (false-kill 2.2%, exact binomial); 50-rollout
  screens can't see <12-13pp; frame screens as SELECTION with a kill band, never significance
  tests (certifying 10pp costs ~280 rollouts/arm).
- **Shortcut kill (from 02, at ep10 + ep50):** D1 null-state/null-vision action-MSE sweep on
  ~100 held-out demos (intact / dataset-mean state / permuted state / background-color images):
  ΔMSE_vision ≈ 0 while ΔMSE_state large → shortcutted → KILL regardless of success. D2
  state→z ridge/MLP probe: conditioned-token R²≳0.9 AND z→object-pose at the state-only floor
  → encoder collapse → kill. Continuous monitors: T1 val-loss INVERSION (v0.4 arm val loss
  >20-30% better than v0.3 baseline early = red flag, not a win), T2 state-pathway grad norm
  dominating vision epochs 1-10 or gate saturating in first epochs.
- **Aux-health gate (from 03, at ep10 ckpt, before any rollout):** (1) zeroing VISION barely
  moves the aux loss → arm is shortcutted → kill; (2) chaseability — aux loss must visibly
  descend (unchaseable target was the E4/torque-aux failure signature).
- Promotion: within/above dense band at ep20 → ep50 confirm; top-2 at ep50 → ep100; single
  winner → big run. Certification (winners only): 100-200 rollouts × 2 seeds, Wilson/UMA
  bounds, disjoint one-sided CIs at 0.975 (STEP or truncated SPRT for sequential stopping:
  20pp bands decide in ~30 rollouts avg at α=β≈0.05).
- **Fast-eval caveat (04 + Consistency Policy data):** reduced-step eval collapses on
  precision tasks — DDiM-15 on Tool Hang: 0.79→0.14. Our DDPM-16 validation (0.64 vs 0.66)
  was on SQUARE only; calibrate per task family before trusting fast eval anywhere else;
  never mix samplers across arms inside one comparison. tool_hang tonight is judged on
  standard DDPM-100 eval (watchers already do this).

## 5.1 DESIGN FREEZE — wave 1 (night-watch draft, 07-31 ~1:55am; Ishneet ratifies in the morning)

Registered BEFORE any wave-1 downstream number exists. Calibration arm was launched first
(dense-patch square_noft, ckpt-every-10, seed 42 = the 07-29 66%-run's seed, run
`2026.07.30/17.49.40`) — it defines the comparator curve and has no v0.4 content.

**Substrate:** robomimic Square (E6-native zarr path), 1 seed (42), bs96/12w, cosine LR,
ckpt-every-10, frozen encoders, identical hparams and eval seeds across arms.
**Screen eval:** fast-eval DDPM-16 × 50 rollouts (validated on Square 0.64-vs-0.66 only —
never compared against DDPM-100 numbers within a comparison).

**Arm list (all vs the calibration curve):**
| arm | encoder ckpt (NAS kepler_ckpt/) | insertion | tests |
|---|---|---|---|
| sa-h6 (LEAD) | square_mc2_v04sa_h6_l0.05 | un-pooled 8 gated tokens | claim A via state-as-query |
| sa-h16 | square_mc2_v04sa_h16_l0.05 | same | horizon sensitivity |
| sb (control) | square_mc2_v03_lp2.0 | un-pooled 8 gated tokens | state-blind control; unpool re-test on clean substrate |
| zo (claim B) | square_mc2_v04sa_h6_l0.05 | 8 z-tokens + lowdim token, NO dense vision | z-only parity at 8-vs-196 tokens |
| rc-pool (retro) | square_mc2_v03_lp2.0 | pooled z as ONE gated token | v0.3 insertion kill re-test |
| rc-cat (retro) | square_mc2_v03_lp2.0 | pooled z CONCAT into per-step cond token | v0.3 concat kill re-test |

NOT in wave 1: λ=0.2 encoder pair (sensitivity, deferred until a lead signal); state-in-K/V +
discrete-target (no pretrained encoder yet — wave 2 builds); force arms (rung-0 tool_hang
failing decisively at ep30: ft 0.0 vs noft 0.52).

**Gates:**
- KILL: arm fast-eval ≤ calibration(same epoch) − 12pp at TWO consecutive reads (ep10+ep20,
  or ep20+ep30). Hard kill: arm ≤5% at ep20 while calibration ≥20% (N1 record: nothing
  ≤5% recovered).
- CONTINUE: within ±12pp of calibration → run to ep50 (screens are SELECTION, not
  significance; gaps <12pp are invisible at 50 rollouts and decide NOTHING).
- PROMOTE: arm ≥ calibration + 12pp at any read → confirm stage (DDPM-100 on the same ckpts,
  continue toward ep100; top-2 arms max).
- Diagnostics at ep10 (flag, not kill): policy-side scalar gate |g| < 1e-4 AND not growing
  by ep20 → tokens unconsumed, investigate before spending rollouts (zo exempt — ungated).
- Claim verdicts at ep50: A-signal = sa-h6 or sa-h16 ≥ calib+12pp (then DDPM-100 confirm).
  B-signal = zo within 7pp of calib (token-matched headline; confirm before claiming).
  Retro surprise = rc-pool/rc-cat within 7pp of calib → pnp concat/insertion kills get
  formally downgraded (per the 07-30 conclusions audit).
- Lit-audit adoptions (RESEARCH.md · 06): any DECISION-GATING positive gets ≥3 seeds before
  it's called a win; tie bands move from fixed ±6-7pp to measured seed CIs at that point;
  scope all wave-1 conclusions "frozen-regime, sim-only, Square, diffusion-BC".

### §5.1 amendment 1 (07-31 ~2:50am, registered BEFORE any same-chassis arm number existed)

sb ep10 = 0.56 exposed a comparator gap: the unpool-family arms (sa-h6, sa-h16, sb, rc-pool,
rc-cat, zo) ride the ResNet-hybrid chassis, which learns Square much faster than the
dense-patch chassis — dense-calibration comparisons conflate chassis with encoder content.
Fix, registered now (sa-h6/sa-h16/rc-* have NO reads yet; zo's ep10 was read but zo is a
B-claim arm judged vs dense):
- NEW `hybrid-base` arm: STOCK DiffusionTransformerHybridImagePolicy (no kepler anything),
  same recipe (bs96, seed 42, ckpt-every-10, fast eval), launches on GPU 7 when the autokill
  frees it. This is the SAME-CHASSIS reference curve.
- Claim-A verdicts (sa-h6/sa-h16 and retro comparisons): judge vs `hybrid-base` at the same
  epoch, same ±12pp bands. Dense calibration remains (a) the claim-B/token-matched comparator
  for zo, (b) the kill floor for obviously-dead arms.
- sb's role sharpens: sb vs hybrid-base isolates what un-pooled v0.3 tokens add to the SAME
  chassis; sa-h6 vs sb isolates the state-as-query delta (the actual v0.4 question).
- Kills already registered stay unchanged; no arm is killed against the dense curve alone if
  it is within band of hybrid-base.

### §5.1 deviation log (07-31 ~5:25am — kill on sight if you disagree)

hybrid-base ep20 = 0.80 completed the two-read kill pairs for sa-h6 (0.28/0.36 vs 0.56/0.80)
and rc-cat (0.10/0.38). DEVIATION: both kept to their ep50 read, then killed regardless
(no tails). Reasoning: GPUs uncontended (7 idle); each arm's whole purpose is its ep50 verdict
(rc-cat = the registered retro-adjudication of the pnp concat kill; sa-h6 ep50 vs sb ep50 = the
state-as-query mechanism verdict); rung-0 tonight showed 20-epoch-delayed convergence is REAL
in this recipe family (ft 0/0/0/40/62). Bug hypothesis for the laggards checked and rejected:
rc-cat train loss ≈ rc-pool (0.074 vs 0.070 — fits fine, fails rollout = the E5 signature);
sa-h6 loss elevated (0.099 vs 0.067) with normal-size gate = tokens actively hard to use, not
ignored garbage. zo's hard-kill stands (different class: 0/50 twice).

## 6. WAVE-1 RESULTS (07-31, complete in one night — see PROGRESS.md for the board)

ep50 fast-eval, matched recipe, 1 seed: hybrid-base 0.90 > sb 0.86 ≈ rc-pool 0.82 ≫ sa-h6 0.54
> rc-cat 0.34 ≫ zo 0.00. sa-h16 killed ep20 (0.26 vs hybrid 0.80). Claims: A NO (parity best),
B NO (0/50 ×2), F rung-0 NULL (both tasks). StateAdd verdict: the state-as-query injection
COSTS −32pp vs its state-blind twin at ep50 on Square — elevated train loss with normal-size
policy gate = the tokens are hard to use, not ignored; both horizons; no shortcut flags offline.
The one positive: v0.3 encoder content reaches chassis parity when inserted as gated tokens —
the v0.3 downstream failures are now fully attributed (pnp noise + concat insertion), NOT
encoder quality. Wave-2 options (morning): C-axis pivot per program kill rule; or escalation
to a non-vision-saturated task for the parity arms; or StateAdd post-mortem (why does
state-conditioned extraction fight the policy? — D2 was clean, so it's not state-echo).

## 7. JQ encoder-rebuild sketch (Slack 07-31 1:10am) — assessment

**The sketch:** per-modality Conv1d-over-T stem (P→d), spatial compress S→n_s (perceiver |
avgpool), temporal resample T→R, then fusion = flatten [B,R,3·n_s·d] → Linear → d → self-attn
over R → pool → [B,d]. Hide-one = zero the modality slice pre-linear; T=1 through the resampler
(one code path); mm_perceiver3 objective unchanged; ee_mask handled in the temporal resampler;
new training script, strict [B,T,S,P]. Follow-up question (Ishneet): big-width fusion
transformer + MLA to handle the flattened dimension?

**MLA verdict: not applicable.** MLA is KV-cache compression for autoregressive decode; the
fusion attention here is one bidirectional pass over R≈8-32 tokens — no cache, nothing to
compress, and training-FLOPs MLA ≈ MHA plus extra projections. The real cost of a wide trunk
is the FFN (~2/3 of ~12·D²/layer; D = 3·n_s·d = 6144 at n_s=8, d=256 → ~450M params/LAYER vs
the whole current encoder at a few M), which MLA doesn't touch. A trunk that size also breaks
the §3 screening economics (2-4h encoder pretrains) and is an overfit risk at our data scale.

**The legit worry underneath:** flatten→Linear is a 24× per-timestep bottleneck (6144→256)
applied BEFORE any temporal attention, and it is the ONLY cross-modal contact point in the
sketch (stems + spatial compress are per-modality) — a late-fusion move against the 07-21
composition evidence (early > single > late).

**Counter-proposal (keeps everything else in the sketch):** don't flatten — after the resample
keep [R, 3·n_s] tokens at width d with modality+slot embeddings, self-attn over R·3n_s ≈ 384
tokens (cheap; fusion becomes multi-round attention instead of one linear), learned-query pool
→ [B,d]. Hide-one = attention-mask the modality's tokens (the existing True=BLOCKED machinery;
zero-slice leaks a learnable "hidden" symbol through the linear's bias); ee_mask rides the same
mask path. Worth adopting from the sketch regardless: strict [B,T,S,P] + one code path incl.
T=1, the T→R resampler (current temporal context grows linearly in T: C·217 tokens), Conv1d
stem as a screenable arm. If fusion capacity is ever the binding constraint: depth at d=256 or
factorized within-tick/across-R attention before width; GQA + narrow FFN over MLA if width.
Reply drafted for JQ 07-31 ~20:10; awaiting thread outcome before any build.

### §7.1 Thread outcome (JQ replies, Slack 08-01 ~7:20am) — R-length, layered cross-attn, dual-path

JQ's three follow-ups: (1) MLA "depends on how long R is gonna be"; (2) re-inject the original
information over multiple layers via cross-attn — self-withdrawn in-thread ("maybe not nvm");
(3) dual-path spatial and temporal — assessment requested.

**R-length gates the joint-vs-factorized choice, never the MLA one.** MLA's payoff is KV-cache
memory during autoregressive decode; a bidirectional single-pass encoder has no cache at ANY R.
What R does gate: under the no-flatten counter-proposal the trunk sees N = R·3n_s tokens
(R=16 → 384, trivial; flash attention keeps joint attn feasible well into the thousands).
Past R ≈ 50-100 unflattened, factorized within-tick/across-R becomes the efficiency move —
already §7's named escalation. And R shouldn't get there: it's the resampler's OUTPUT rate,
ours to choose (8-32 in every sketched use); absorbing long inputs (100Hz force windows) is
the resampler's job, not the trunk's.

**Layered cross-attn re-injection: agree with the withdrawal, for a keepable reason.** It's the
Perceiver-IO/Flamingo pattern — the correct escalation IF the single cross-modal contact point
proves lossy. The no-flatten variant keeps per-slot tokens in the stream and buys most of the
same thing with zero new machinery. Shelve, don't kill.

**Dual-path: two readings, one verdict — not first.** (a) Alternating within-tick / across-R
layers (DPRNN/SepFormer-style ≈ TimeSformer divided attention): the right escape hatch when N
binds, but at N≈384 joint attention is affordable and strictly more expressive — factorizing
removes cross-(tick,slot) interactions we can afford to keep. (b) Two parallel branches
(spatial-first ∥ temporal-first) merged at the end: structurally the late-fusion shape the
07-21 composition analysis penalized (early > single > late; per-view-encode+avg lost to the
best single view) — separately pooled summaries can't cross-talk during extraction. Steelman
for (b): per-tick spatial compress is motion-blind; a temporal-first path keeps per-location
dynamics. But the sketch's own Conv1d-over-T stem already gives every token LOCAL temporal
context before spatial compress, and the residual risk (long-range per-location dynamics dying
in the per-tick compress) is a 2-4h probe on the first build, not a second path: future-force /
dSE3@0.25s readout from R-tick z vs per-frame carry. Build single-path joint first;
pre-register dual-path as the arm added only if that probe fails.

**Thread-strategy note:** all three follow-ups circle the same underlying worry — one lossy
contact point between modalities and trunk — which is exactly what §7's counter-proposal
(don't flatten; [R, 3n_s] tokens + mask-based hide-one) addresses, and the visible thread
never received it (the 8:15/8:44pm replies covered shape agreement + the MLA pushback only).
This is the moment to surface it.

**Update 08-01 ~3:41-3:44pm: counter-proposal SENT** (Ishneet posted: R is ours / no cache at
any R; all three ideas = the flatten→linear lossy contact point; no-flatten [R, 3n_s] tokens +
learned-query pool; joint attn affordable, parallel-branch merge = the late fusion we already
falsified). Still NOT in the thread: (1) hide-one semantics under no-flatten — zero-slicing no
longer works (linear bias leak), must become an attention mask on the modality's tokens, ee_mask
same path — JQ will hit this the moment he specs the build; (2) the stem point — JQ's own
Conv1d-over-T already gives tokens local temporal context pre-compress; (3) the
alternating-layers reading of dual-path as the pre-registered big-R escape hatch (completes the
"depends how long R is" answer: R gates joint-vs-factorized, never MLA); (4) the dual-path
adjudication probe (below).

### §7.2 Settled rebuild spec (08-01, post-thread) — keep / change / drop / gate

**KEEP (JQ's sketch as-is):** strict [B,T,S,P] contract + ONE code path incl. T=1 through the
resampler · per-modality Conv1d-over-T stem P→d (also buys local motion context BEFORE the
spatial compress — covers most of what a temporal-first path would add) · Order A: spatial
S→n_s (config perceiver | avgpool) then temporal T→R · R = the resampler's OUTPUT rate, ours
to set (8-32, default 16) · mm_perceiver3 objective carried over unchanged · new training
script.

**CHANGE (2):**
1. **Fusion: NO FLATTEN.** After the resample keep [R, 3·n_s] tokens at width d with
   modality+slot embeddings, self-attn over all R·3n_s tokens (≈384 @R=16 — trivial;
   flash-attn keeps joint feasible into the thousands), learned-query pool → [B,d]. Removes
   the 6144→256 single lossy contact point — the one worry under all three of JQ's follow-ups
   (MLA, layered cross-attn, dual-path) — and keeps fusion early/multi-round per the 07-21
   composition evidence.
2. **Hide-one: attention MASK, not zero-slicing.** Zeroed slices leak a learnable "hidden"
   symbol through downstream biases; mask the modality's tokens with the existing True=BLOCKED
   machinery; ee_mask rides the same mask path.

**DROP:** MLA (no KV cache exists at any R — one bidirectional pass) · the wide fusion trunk
(the width problem disappears un-flattened; its cost was the FFN, which MLA never touched
anyway) · dual-path as a build item · layered cross-attn re-injection (SHELVED, not killed —
Perceiver-IO pattern, mostly free once per-slot tokens stay in the attention stream).

**PRE-REGISTERED GATES (registered now, before any build number exists):**
1. **Dual-path adjudication = the v0.2 gate, no grandfathering:** future-force @t+h and
   dSE3@0.25s read out from the R-tick z must be ≥ the same probes on plain per-frame z
   (v0.2's temporal encoder FAILED exactly this: future force 0.10 vs single-frame 0.17).
   Fail → the per-tick spatial compress is eating dynamics → the temporal-first path earns a
   build. Pass → dual-path stays dead. ~2-4h on the existing probe harness; kill-only usage.
2. **Scale trigger:** joint attention binds only past R ≈ 50-100 unflattened; the fix then is
   ALTERNATING within-tick/across-R layers (factorized), never parallel branches
   (parallel-merge = the late fusion 07-21 falsified).
3. Build must fit §3 screening economics (2-4h encoder pretrains) and pass the standard
   battery (state R², RankMe, force probe holds) like every other arm.

**Downstream relevance (claims-ledger mapping — honest read):** the rebuild does NOT re-open
A or B — wave 1 showed encoder content already at chassis parity; the downstream failures were
insertion mode, state-conditioning, and task saturation, none of which this touches. F: the
multi-rate resampler is the missing machinery for a REAL force-fusion arm (100Hz force vs
20Hz vision — E7's "real case"), but claim F stays gated on a rung-0 pass somewhere. **C is
where it pays:** a windowed encoder emitting compact temporal z is the natural substrate for
latent-MPC (CompACT-style token planning, DEEPRESEARCH_NEXT #3) and the predictor line — the
rebuild aligns with the live exit axis rather than re-fighting dead claims. Priority
unchanged: shift-robustness eval (eval-only, #1) is still the recommended first wave-2 move;
the rebuild proceeds in parallel as JQ's build.

### §7.3 Thread outcome pt. 2 (JQ replies ~4:00-4:02pm, seen 08-02) — no-flatten ACCEPTED, MLA closed, one NEW idea: sum-fusion

**Settled in-thread:** JQ accepts the no-flatten shape ("B,R,3*n_s,d is dooable" → §7.2
CHANGE 1 adopted) and formally agrees MLA isn't needed ("i agree we don't exactly need MLA
here") — the R-length question closes as assessed (R gates joint-vs-factorized only, never
MLA). Ishneet's 3:50pm message delivered the conv-stem point (item 2 of §7.1's unsurfaced
list). Still unsurfaced: hide-one-as-mask (item 1 — due the moment the build gets specced),
the alternating-layers reading of dual-path (item 3), the adjudication probe (item 4).

**NEW from JQ:** "actually i'm thinking if modality need attention... would be interesting if
after we align we just like sum the modality" — read as: after alignment to width d, sum
element-wise across the modality axis ([R, 3, n_s, d] → [R, n_s, d]); the trunk then runs on
R·n_s tokens with NO cross-modal attention.

**Assessment:**
1. Sum is a fixed, content-independent mixer — a strict special case of what the token trunk
   can learn (uniform cross-modal attention ≈ sum). It cannot weight modalities by content.
2. Slot-correspondence problem: after per-modality perceiver compress, slot i of vision and
   slot i of force share no semantics (independent learned queries, unordered) — sum imposes
   an arbitrary pairing training must co-adapt around.
3. Family evidence against: the 07-21 composition analysis falsified fixed pooling across
   views (avg-probe lost to best single view; parallel-merge = late-fusion penalty). This sum
   sits EARLIER (trunk still mixes time/slots after it), so it is not the identical falsified
   config — screenable, not dead on arrival — but the prior points the same way.
4. In its favor, honestly: hide-one becomes trivial (omit the modality from the sum — no mask
   machinery, no bias-leak issue at all), token count drops 3× (N = R·n_s = 128 at defaults),
   and a match would itself be the answer to JQ's actual question (modalities don't need
   attention here).
5. **Resolution — config flag, not a design fight:** `fusion: tokens | sum` at the
   post-alignment merge, same build, both variants screened in the §3 fast loop under the same
   probe battery + §7.2 gate 1. Free diagnostic: in the tokens variant, cross-modal attention
   collapsing toward uniform ⇒ sum was sufficient — readable from attention maps, zero rollouts.

**Pre-registered (before any build number exists):** sum-fusion is adopted only if it matches
the tokens variant on the standard battery (state R², RankMe, force probe, dSE3@0.25s) and
gate 1; a genuine tie breaks toward sum (cheaper, cleaner hide-one). Hide-one semantics per
variant: tokens → attention mask (§7.2 CHANGE 2); sum → omit-from-sum.

### §7.4 PROPOSED additions from the Enigma "obsessed encoder" read (08-02 — Ishneet to ratify; all kill-only)

**Threat-model generalization:** axis D guards ONE dominant condition (robot state). Enigma
collapses DINOv3 + LeJEPA + LeWM (our Build-2 recipe source) onto ANY low-entropy channel that
is constant within a view/window and predictable across them — SIGReg satisfied throughout,
loss crossover as the tell, probes peak-then-decay. Our analogs: camera identity (MEASURED
dominant, 07-21 ratio 1.46), episode-static content (initial poses = the RandGoal analog),
static background. The temporal rebuild is MORE exposed than per-frame encoders (a static
feature is perfectly predictable across a window — the ideal shortcut for any
future-prediction objective; candidate mechanism for the v0.2 temporal encoder's force-probe
regression).

**Nuisance-dominance battery (extends funnel stage 1 + §7.2 gate 3; cached-z, minutes-hours):**
- N1 camera-ID probe on z — accuracy + variance share (formalizes the 1.46 finding as a detector)
- N2 episode-ID / initial-pose probe — the RandGoal analog
- N3 nuisance-vs-content variance decomposition (z variance explained by camera/episode vs
  object pose)
- N4 encoder-side loss crossover — pretrain val loss meaningfully BELOW the control sibling =
  flag, never a win (T1's logic moved to pretrain; Enigma + NADA both show the shortcut arm
  WINS on loss)
- N5 static-pattern canary (rebuild smoke battery + new objective families only): inject a
  constant-within-episode pattern, watch loss crossover + probe peak-then-decay + z-similarity
  clustering by pattern; ~hours, 1 GPU; Enigma's code is public for the exact recipe

**Elevation, not addition:** predictor-beats-carry (v0.2's +28-36% control) is THE
anti-stagnant-watermark gate for any predictive objective — a collapsed predictor equals
carry. Mandatory for the rebuild and any future predictor line.

**Explicit non-adoptions (same evidence):** no diversity/rank term in the objective (LeJEPA
carries SIGReg and collapses; INTACT rank inversion; RankMe stays kill-only) · no clustering
as anti-collapse (DINOv3's Sinkhorn+KoLeo collapsed; marginal machinery, same family) —
discrete/clustered z stays a TARGET/variant inside deep-research #3/#4.

**Writeup ammo (bank):** Enigma loss-crossover + NADA inversion + INTACT frozen-head + our 3×
probes-lie = externally corroborated principle: objectives get satisfied by the most
predictable channel; marginal regularizers don't select content; selection must come from the
objective (action grounding / residual-chasing / masking the predictable channel).


---

> **APPENDIX (merged 2026-08-02 during doc consolidation, content unchanged). Formerly `V04_BUILD_20260730.md`.**

# v0.4 StateAdd build log — 2026-07-30

Implementation of the V0.4.md §2 frozen design: axis-A rank-1 (StateAdd), axis-B rank-1
(action-conditioned future-vision-latent, continuous cosine), axis-C (dedicated predictor
queries), axis-D (full anti-shortcut stack). Code only — NO training runs launched beyond
the 300-step smoke below. No git commands run.

## Files

| File | What |
|---|---|
| `world_tokenizer/robocasa_pretrain_v04.py` | NEW pretrain script + `MMPerceiverMCStateAdd` (subclasses v0.3 `MMPerceiverMCPatch`; v0.3 script untouched). Also shared data helpers (`load_actions`, `build_future_and_actions`, `pool_patches`, `infer_stride`). |
| `world_tokenizer/v04_equiv_test.py` | The α=0 ≡ v0.3 correctness gate (T1) + invariant tests T2-T4. |
| `world_tokenizer/v04_detector.py` | D1/D2 shortcut detectors from 02_shortcut_mitigation §4, standalone, offline, verdict table. Auto-detects v0.3 vs v0.4 ckpts. |
| `world-encoder/V04_BUILD_20260730.md` | this log |

Env: `/home/menlo/brain/ishneet/robocasa/.venv/bin/python3` (torch 2.7.1+cu126,
stable_pretraining, sklearn all present; world-encoder has no live venv). Smokes on GPU 0/1
only. All ckpt outputs default to `/mnt/nas/data/robocasa/kepler_ckpt/` (local disk 91%).

## Architecture (as frozen, deltas vs v0.3)

- **A — StateAdd:** `state_mlp = MLP(16→256→8·256)` → reshape `[8,256]`;
  `q_i' = q_i + qmask_i · tanh(α_i) · u_i`, `α ∈ R^8` zero-init. Queries stay learnable.
- **B — 4/4 split:** `cond_qmask` buffer = `[1,1,1,1,0,0,0,0]` — queries 0-3 conditioned,
  4-7 never see state (02 §3 recipe, order per its §5 pseudocode).
- **C — predictor queries:** P=16 learned queries concatenated with the 8 output queries
  through the SAME PerceiverFuse blocks over the same context, state K/V column masked for
  all 24 rows. PerceiverFuse has no query self-attention (queries independent per-row in
  `CrossAttention`), so read-only isolation is structural — verified numerically (T3).
  Predictor queries receive `pred_state(s) + pred_act(action_chunk)` broadcast-added.
  Loss reads predictor outputs after the final (2nd) block only.
- **D — objective:** target = RAW frozen ViT-B/16 per-cam patch-pooled features `[K,768]`
  of the cache row at `t + h_eff` (stop-grad by construction — comes straight from the
  fp16 patch cache, same frozen backbone the encoder consumes). Head:
  `pred_head = MLP(256→1024→K·768)` on the mean-pooled predictor outputs; loss =
  `mean_cams(1 − cos)`; total = v0.3 loss + `lamb_aux · aux`, `lamb_aux` default 0.05.
- **E — anti-shortcut stack** (all config-gated, defaults ON): per-dim `F.dropout(p=0.2)`;
  Gaussian noise σ = 10^(−SNR/20) on the normalized state (default SNR 20 dB → σ=0.1);
  CFG whole-condition dropout p cosine-annealed 0.5→0.1 over the first half of
  `total_steps` then constant, null = learned `[8,256]` token; zero-init gates.
- v0.3 losses (`inv + λ_sig·sig + λ_patch·patch`) kept verbatim; `z_from_s`, `z_full`,
  sigreg paths use plain (unconditioned) queries exactly as v0.3.

## Design choices where the docs left options open (each recorded per task brief)

1. **v0.3 loss stack kept unchanged** (inv + sig + patch) with the aux ADDED. Recorded
   risk: `pred_s(z_from_v)` becomes partially copyable once gates open (01 §6.3).
   Mitigants: condition dropout makes it non-copyable on 10-50% of samples; D2 is the
   watchdog; first lever on a D2 collapse flag = drop the `pred_s` term.
2. **Horizon snapping:** cache is stride-5 at 20 Hz, so `h_eff = round(h/5)·5` (min 5):
   h=6 → 5 ticks (0.25 s), h=16 → 15 ticks (0.75 s) — inside 03's {~0.3 s, ~0.8 s} band.
   The action chunk covers exactly `t..t+h_eff` (coherent with the target frame).
3. **Aux target = RAW 768-d backbone features** (hard-frozen), NOT the EMA `tgt_v`
   projection — 03 §4a: frozen external targets need no EMA; one target family only.
4. **P→target mapping:** 03 gives no per-token matching for our shapes; chose mean-pool
   over the 16 predictor outputs → MLP projector → K×768, per-cam cosine (Octo readout
   pooling + FRAPPE projector+cosine recipe).
5. **Predictor conditioning ungated and never condition-dropped:** the zero-init-gate
   mandate (01 §6.1) covers state paths into the policy-facing z; the predictor is
   training-only machinery (GeoPredict pattern) and must ALWAYS see state+actions so the
   residual the loss chases is scene content. Per-dim dropout + Gaussian noise DO apply
   (shared regularized state `s_reg` feeds both new paths). v0.3 loss paths get clean
   state, exactly as v0.3.
6. **Predictor K/V:** same context with the state column masked for predictor rows too —
   state reaches the predictor only via query injection (01 §6.2: state out of the value
   path everywhere).
7. **Condition-dropout null at the injection level** (learned `[8,256]` token replacing
   `u = state_mlp(s)`), not at the raw-state level; flows through the same tanh(α) gates,
   so α=0 equivalence is preserved trivially.
8. **Actions used raw** (already normalized to [−1,1] in the lerobot data, 12-D);
   end-of-episode: future row clamped to the episode's last cached frame (the E4
   `min()` pattern from `diffusion_policy/dataset/lerobot_dataset.py`), action chunk
   padded by repeating the last action.
9. **`embed_vision` (gates battery) returns the CONDITIONED pooled latent** at learned α
   (eval: no dropout/noise/cond-drop) so the v0.3 `gates()` battery runs unmodified;
   interpretation of `zv_r2_state` on conditioned tokens defers to D2.
10. **Equivalence reference** = `MMPerceiverMCPatch.fuse(ctx, mask_state, pool=False)` —
    the pretrain-side ground truth of what `kepler_encoder.forward_set()` computes
    (masked state token ≡ excluded state token under softmax masking; the downstream
    ckpt-loading path `proj_v/fuse/mod[0]/cam_emb` is untouched by this build).
11. **Detector D1 measured on encoder outputs + aux loss** (no policy head exists at
    pretrain time): relative L2 latent deltas per query subset + the 03 aux-health sweep
    (null-vision must move the aux; also permuted-actions as an action-use check). D2 =
    RidgeCV state→z per-token R², episode-held-out. The z→object-pose floor from 02 is
    NOT computed (no object pose in the cache) — flagged in the printout. Thresholds
    (vision-delta <0.02 ∧ state-delta >0.10 → SHORTCUT; cond R² ≥0.9 → COLLAPSE) are
    02's proposals, printed alongside raw numbers.

## Equivalence test result (the main correctness gate) — PASS

`v04_equiv_test.py` on the REAL ckpt `/mnt/nas/data/robocasa/kepler_ckpt/pnp_mc3_v03_lp2.0/seed0.pt`,
256 real cache rows, fp32, GPU 0:

```
T1 alpha=0 equivalence:   max|diff| = 0.000e+00  PASS (tol 1e-6)   <- bit-identical
T2 state invariance @a=0: max|diff| = 0.000e+00  PASS (tol 1e-6)
T3 predictor isolation:   max|diff| = 3.815e-06  PASS (tol 1e-5)   <- 24-vs-8-query kernel tiling only
T4 4/4 split @a=0.5:      cond-shift 6.284e+00, uncond-shift 0.000e+00,
                          state-sens cond 5.676e+00, uncond 0.000e+00  PASS
ALL PASS
```

v0.3 ckpt loads with strict=False → 0 unexpected keys, 20 missing keys all v0.4-new
(`state_mlp/alpha/cond_qmask/null_u/pred_q/pred_state/pred_act/pred_head`).

## GPU smoke (300 steps, GPU 0) — PASS

`--h 6 --lamb-aux 0.05 --max-steps 300 --epochs 4 --no-gates`, batch 256, fresh init,
defaults for the anti-shortcut stack. Full log: `/tmp/v04_smoke.log`; ckpt kept at
`/mnt/nas/data/robocasa/kepler_ckpt/pnp_mc3_v04sa_SMOKE_h6_l0.05/` (validates the
detector's v0.4 path — safe to delete).

```
step  25: loss 4.5796 aux 0.2461 p_drop 0.473 |tanh a|cond 0.00958 grad[alpha 4.32e-03 s_mlp 9.18e-05 proj_v 2.73e+00]
step 100: loss 2.6052 aux 0.0621 p_drop 0.200 |tanh a|cond 0.11833 grad[alpha 5.38e-02 s_mlp 8.47e-03 proj_v 1.20e+00]
step 200: loss 1.7781 aux 0.0499 p_drop 0.100 |tanh a|cond 0.18751 grad[alpha 1.94e-02 s_mlp 1.19e-02 proj_v 4.76e-01]
step 300: loss 1.8941 aux 0.0431 p_drop 0.100 |tanh a|cond 0.21460 grad[alpha 1.70e-02 s_mlp 1.53e-02 proj_v 3.81e-01]
```

- Total loss descends 4.58 → ~1.8 (the tail flat/uptick is the EMA-target inv/patch
  terms fluctuating + a 21-batch partial final epoch; per-term aux is MONOTONE
  0.246 → 0.043 — chaseable, the E4-failure signature is absent).
- α gates receive gradient from step 1 (norm 4e-3 → 5e-2 → 2e-2) and OPEN:
  |tanh α| conditioned 0.010 → 0.215; unconditioned α stay exactly 0 (masked, as
  designed). Vision-pathway grad (proj_v) dominates state-pathway grad (s_mlp) by
  ~25-3000× throughout — no GAP T2 signature at smoke scale.
- Cond-drop anneal verified: p 0.473 → 0.100 by half of total_steps, then constant.

## Detector smoke — PASS on both ckpt families

On the REAL v0.3 ckpt (`pnp_mc3_v03_lp2.0/seed0.pt`, GPU 1, 2048 held-out rows /
39 episodes, ~4 min incl. cache load):

```
D1 rel-latent-delta:  null_state 0.0000 | perm_state 0.0000 | null_vision 1.0502
D1 verdict: OK (vision moves the latent)      [state = 0 by construction in v0.3 wiring]
D2 state->z ridge R^2/token: [0.049 0.214 0.097 0.280 0.190 0.086 0.038 0.048]  -> OK
OVERALL: no shortcut flags
```

On the 300-step v0.4 SMOKE ckpt (validates every v0.4 detector path):

```
tanh(alpha) = [-0.213 -0.214 -0.216 +0.216 | 0 0 0 0]
D1: null_state cond 0.8848 / uncond 0.0000 ; perm_state cond 1.0524 / uncond 0.0000 ;
    null_vision cond 0.5425 / uncond 1.1295          -> OK (vision moves the latent)
D1-aux: intact 0.0472 | null_state +0.0064 | null_vision +0.0466 | perm_actions +0.0028
        -> OK (aux runs on vision: zeroing vision moves the aux 7x more than zeroing state)
D2: cond q0-3 R^2 0.701 vs uncond q4-7 0.075 (<0.9)  -> OK (the designed subset contrast)
OVERALL: no shortcut flags
```

## Launch commands for the real pretrains (NOT launched)

Two pre-registered configs (h=6/λ=0.05 and h=16/λ=0.05), v0.3 protocol otherwise
(40 epochs, batch 256, lr 1e-3, λ_patch=2.0, seed 0, full gate battery), one GPU each,
outputs on NAS:

```bash
cd /home/menlo/brain/ishneet/world-encoder
DS=/mnt/nas/data/robocasa/datasets/v1.0/target/atomic/PickPlaceCounterToCabinet/20250811/lerobot
PY=/home/menlo/brain/ishneet/robocasa/.venv/bin/python3

CUDA_VISIBLE_DEVICES=0 nohup $PY -m world_tokenizer.robocasa_pretrain_v04 \
  --h 6  --lamb-aux 0.05 --dataset $DS > /tmp/v04sa_h6.log  2>&1 &
CUDA_VISIBLE_DEVICES=1 nohup $PY -m world_tokenizer.robocasa_pretrain_v04 \
  --h 16 --lamb-aux 0.05 --dataset $DS > /tmp/v04sa_h16.log 2>&1 &
```

→ ckpts land in `/mnt/nas/data/robocasa/kepler_ckpt/pnp_mc3_v04sa_h6_l0.05/seed0.pt` and
`..._h16_l0.05/seed0.pt` (+ `results.json` with h_eff/σ/α_final/gates/state μ,σ).

Optional warm-start variant (design allows it; NOT the default — fresh matches the v0.3
matched-budget protocol): append
`--init-ckpt /mnt/nas/data/robocasa/kepler_ckpt/pnp_mc3_v03_lp2.0/seed0.pt`.

Post-run detector (either ckpt):

```bash
CUDA_VISIBLE_DEVICES=0 $PY -m world_tokenizer.v04_detector \
  --ckpt /mnt/nas/data/robocasa/kepler_ckpt/pnp_mc3_v04sa_h6_l0.05/seed0.pt --dataset $DS
```

## Open items / risks for tonight

- The `pred_s` copyability risk (choice 1) — watch D2 + T1/T2 monitors at ep10.
- σ* (NADA Wasserstein) not computable pre-rollout; 20 dB SNR bootstrap in use.
- Gate-health readout: per-epoch log prints `|tanh α|` of the conditioned queries + grad
  norms of α / state_mlp / proj_v (the T2 GAP monitor) — a dead gate or state-grad
  domination is visible from the training log alone.
